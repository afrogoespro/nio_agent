/** Hard cap on people returned per Apollo search (beta / cost control). */
export const APOLLO_MAX_RESULTS = 10

/** Plan preview only needs one match; still fetched via the same API. */
export const APOLLO_PLAN_SAMPLE_SIZE = 1
