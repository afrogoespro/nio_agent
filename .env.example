# Vercel project settings (server only, never expose to browser)

# Resend: test emails and future drip send
# https://resend.com/api-keys
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev

# Apollo: optional server wide key (users can also paste their own in the app)
# https://app.apollo.io/#/settings/integrations/api
APOLLO_API_KEY=
